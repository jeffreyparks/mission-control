# Dependency overrides for `uv tool install`.
#
# python-jobspy pins NUMPY==1.26.3 and pandas<3, but across its whole source it
# only calls pd.DataFrame, pd.concat and np.round - stable on numpy 2, so numpy
# is raised. pandas stays below 3, matching pyproject's [tool.uv] table.
# Verified against python-jobspy 1.1.82.
#
# `uv sync` / `uv run` read the same overrides from [tool.uv] in pyproject.toml.
# `uv tool install` does NOT read that table, so it needs this file:
#
#   uv tool install --editable . --overrides tool-overrides.txt
#
# tests/test_jobspy_source.py fails if this file and pyproject.toml disagree.
pandas>=2.1.0,<3.0.0
numpy>=2.1
